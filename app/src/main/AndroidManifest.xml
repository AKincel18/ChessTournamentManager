<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adam.chesstournamentmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.CreateTournament"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.AddNewPlayer"
            android:screenOrientation="landscape"
            android:theme="@style/AppTheme.PopUp" />
        <activity
            android:name=".activities.ConfigureTournament"
            android:screenOrientation="landscape" />
        <activity android:name=".activities.Tournament" />
        <activity android:name=".activities.RoundResults" />
        <activity android:name=".activities.FinalResults" />
    </application>

</manifest>